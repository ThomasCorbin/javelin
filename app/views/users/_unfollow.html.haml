= form_for current_user.user_relationships.find_by_followed_id(@user), |
  :html => { :method => :delete }, :remote => true do |f|                          |
  .actions= f.submit "Unfollow", :class => 'fancy_button'
