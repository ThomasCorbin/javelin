= form_for current_user.user_relationships.   |
  build(:followed_id => @user.id), :remote => true do |f| |
  %div= f.hidden_field :followed_id
  .actions= f.submit "Follow", :class => 'fancy_button'
