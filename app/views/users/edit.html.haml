%h1 Edit user

= form_for(@user) do |f|
  = render 'fields', :f => f
  .actions
    = f.submit "Update", :class => 'fancy_button'

%div
  = gravatar_for @user
  = link_to 'Change', 'http://gravatar.com/emails', :target => "_blank"

