source 'http://rubygems.org'

gem 'rails', '3.0.3'

# Bundle edge Rails instead:
# gem 'rails', :git => 'git://github.com/rails/rails.git'

gem 'pg'
gem 'gravatar_image_tag',     '1.0.0.pre2'
gem 'will_paginate',          '3.0.pre2'
gem 'haml'

gem 'compass',                ">= 0.10.6"
gem 'compass-960-plugin'
gem 'compass-colors'
gem 'fancy-buttons'


group :development do
  gem 'rspec-rails', '2.3.0'
  gem 'faker', '0.3.1'
  gem 'ori'

  # run 'lp "foo"' and it will printout a table of methods and where they come from.
  gem 'looksee'

  # puts a header at the top of a model showing what the table looks like.
  # just run annotate at the project root
  gem 'annotate-models', '1.0.4'

  # makes printouts of objects look great. Organized and colorized
  gem 'awesome_print'

  # makes printouts of objects look great. colorized, but not organized
  gem 'wirble'

  # puts objects into a table output.
  gem 'hirb'
end

group :test do
  gem 'rspec',              '2.3.0'
  gem 'webrat',             '0.7.1'
  gem 'spork', 			        '0.8.4'

  # Helps create test data.
  gem 'factory_girl_rails', '1.0'
end

