%h1 Edit user

= form_for(@user) do |f|
  = render 'fields', :f => f
  .actions.button
    = f.submit "Update"

%div
  = gravatar_for @user
  = link_to 'Change', 'http://gravatar.com/emails', :target => "_blank"

