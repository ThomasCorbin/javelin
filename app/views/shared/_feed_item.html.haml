%tr
  %td.gravatar
    = link_to gravatar_for(feed_item.user), feed_item.user
  %td.micropost
    %span.user
      = link_to feed_item.user.name, feed_item.user
    = render :partial => 'shared/micropost_content', :locals => { :micropost => feed_item}
